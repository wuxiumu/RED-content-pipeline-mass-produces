# 03 · 执行智能体 Executor

> 出成品。消费 judged.json 的 wordcard 队列,流水线产出 9 页蜡笔风图文笔记;video/resource 轨后置。
> 全流程断点续跑:每个词的台词、原图、卡片、笔记分别落盘,重跑自动跳过已完成项。

## 1. 定位

- 输入:`data/judged.json` + `assets/style/*` + `assets/wordlists/*`
- 输出:
  - `data/scripted.json`:每词 4 句台词 + 4 条分镜 + 互动话术
  - `data/images/{word_id}/raw_1..4.png`:无文字蜡笔原图
  - `data/images/{word_id}/cards/01..09.png`:可直接发布的 9 页卡
  - `data/produced.json`:标题/正文/标签/图片路径/CTA 全套发布物料
- 不做:不选题、不抓数据、不自动发布(第一阶段人工发布)

## 2. 四段流水线

### 2.1 build_scripts.py:词 → 台词 + 分镜

固化 b.md 的教研 prompt(JSON 模式,一次调用产一个词):

```
你是有 10 年经验的幼儿英语启蒙教研专家与短视频编剧。
为单词【{en} {zh}】生成 15-20 秒磨耳朵内容,严格输出 JSON:
{
  "lines": [
    {"en": "This is a/an {en}.", "zh": "这是一个{zh}。"},
    {"en": "The {en} is ...",  "zh": "..."},
    {"en": "Its color is ...", "zh": "..."},
    {"en": "I like this {en}.", "zh": "..."}
  ],
  "phonetic": "/.../",
  "scenes": ["分镜1画面:主体造型+动作+背景,中文,不含任何文字字母", "...", "...", "..."],
  "interaction": "一句亲子互动话术(如:逛超市时让孩子找一找{zh}在哪里)"
}
规则:每句英文 ≤8 个基础单词;句式=命名→特征→颜色→喜好;
scenes 必须描述蜡笔手绘画面但不得要求出现任何文字/字母。
```

- 第 2 句特征从词族表 `slot` 延伸(round / long / soft…),由 LLM 选词
- JSON 模式成功率:zhipu 用 `response_format=json_object`(参考 _plan 教训,从 25% → 100%)
- 每词独立存盘,失败重试 2 次,不阻塞后续词

### 2.2 gen_images.py:固定画风骨架批量生图

每张图 prompt = `assets/style/base_style.txt`(不可变骨架)+ 词族表 `slot`(主体插槽)+ 该句 `scenes[i]`;
负面词统一附加 `assets/style/negative.txt`。

**base_style.txt 骨架(全系列逐字不变)**:
> 儿童蜡笔手绘绘本插画,粗蜡笔笔触质感,手绘肌理,童真可爱,3:4 竖画幅,平视视角;圆圆的黑色豆豆眼睛,弯弯微笑嘴巴,粉色圆形腮红,简单黑色细线短手短脚;嫩绿色蜡笔草地,明亮浅蓝蜡笔天空,马卡龙柔和配色,画面干净简约,低幼绘本,无文字无字母无水印,高清。

**negative.txt**:
> 写实,照片,3D 渲染,阴暗,复杂背景,阴影过重,扭曲五官,尖锐线条,文字,水印,字母,数字,人脸,多余物体,杂乱装饰

一致性手段(按成本递增):
1. 同词 4 张共用同一段 `slot` 描述,主体措辞逐字一致
2. 首张满意图作为图生图参考(百炼支持参考图时开启 `--ref`),后 3 张基于参考图
3. 固定 seed/模型版本;出图后人工挑一张"定妆照"存入 `assets/hero/{en}.png`,后续 video 轨复用

调用:`bl image generate --prompt ... --size portrait_4_3 --out-dir ... --quiet`,失败重试 2 次仍失败标 `image_status: failed`。

### 2.3 compose_cards.py:Pillow 排 9 页卡(1080×1440)

| 页 | 底图 | 版式 |
|---|---|---|
| 01 封面 | raw_1(裁切铺满) | 顶部半透明奶白圆角条 + 大字标题(标题文案在 execute 阶段生成,此处先用词名占位也可重渲染);底部胶囊条"每天5分钟磨耳朵 · 水果系列 01" |
| 02-05 | raw_1..4 | 底部 30% 奶白圆角板:英文圆润大字(3 行内自动缩放),中文小字在下;右上角小页码 |
| 06 单词页 | 马卡龙纯色 + raw_1 小图点缀 | EN 超大圆体居中 + 音标 + 中文 |
| 07 互动页 | 纯色 | 标题"今天就这样玩" + interaction 文案(自动换行)+ 小提示 |
| 08 合集页 | 纯色 | 词族 checklist:已更新词打勾,引导"点主页合集追更" |
| 09 CTA 页 | 纯色 | "原创磨耳朵资源包已整理好(自制动画+可打印闪卡)· 评论区扣【1】",无微信无外链 |

- 字体:英文 `Arial Rounded MT Bold`(/System/Library/Fonts/Supplemental/),中文 `PingFang SC`;脚本内置字体搜索与缺失报错
- 颜色常量集中在脚本顶部(奶白 #FFF8EC、主色等),全系列统一
- 文字安全区:左右边距 80px、底部 200px,避开小红书 UI 遮挡
- 输出 `data/images/{word_id}/cards/01.png … 09.png`

### 2.4 execute.py:组装发布物料

每个词一次 LLM 调用(JSON 模式)产出:
- `titles`:5 个封面标题候选(公式:年龄锚点+结果承诺+低成本动作+情绪/悬念),LLM 按点击率自评返回 `pick_index`
- `body`:200-400 字正文,骨架=痛点钩子 → 怎么用(每天 5 分钟跟读)→ "先收藏免得找不到" → 评论区福利
- `tags`:6-8 个,前 3 必须是搜索大词
- `post_time_hint`:默认 20:30-22:00

固定标签池(在 collect_config.yaml):
`#英语启蒙 #幼儿英语启蒙 #磨耳朵 #亲子英语 #英语单词 #宝妈必备 #英语启蒙这样做 #育儿日常`

合规后处理(代码强制,不靠 LLM 自觉):
- 正文/标题正则拦截 `微信|vx|v信|二维码|http|网盘|盗版|小猪佩奇|奥特曼|汪汪队|宝宝巴士|哆啦` 等,命中重生成,2 次仍命中则 skipped
- CTA 统一模板:"原创资源已整理,评论区扣【1】,看到都会回"

## 3. 数据结构

### scripted.json
```json
{
  "built_at": "2026-09-11T21:00:00+08:00",
  "words": [
    {"word_id": "wc_001", "family": "fruits", "en": "apple", "zh": "苹果",
     "phonetic": "/ˈæpəl/",
     "lines": [{"en": "This is an apple.", "zh": "这是一个苹果。"}],
     "scenes": ["...", "...", "...", "..."],
     "interaction": "吃水果时让孩子找一找 apple 在哪里",
     "status": "scripted"}
  ]
}
```

### produced.json
```json
{
  "produced_at": "2026-09-11T21:20:00+08:00",
  "posts": [
    {"topic_id": "wc_001", "track": "wordcard", "status": "ready",
     "title": "2岁娃看完主动开口|每天5分钟苹果英语启蒙",
     "body": "...", "tags": ["英语启蒙", "..."],
     "image_paths": ["data/images/wc_001/cards/01.png", "...09.png"],
     "raw_image_paths": ["data/images/wc_001/raw_1.png", "..."],
     "cross_sell_hint": "原创资源评论区扣【1】",
     "collection": "水果系列",
     "post_time_hint": "20:30-22:00",
     "note_url": ""}
  ]
}
```

## 4. 端到端运行

```bash
# 01 市场(冷启动可跳过)
python3 scripts/expand_keywords.py
python3 scripts/scrape_xhs.py

# 02 排产(无样本时自动回退词族表,--limit 控制当日产量)
python3 scripts/judge.py --limit 3

# 03 生产(每步都可独立重跑,自动跳过已完成)
python3 scripts/build_scripts.py
python3 scripts/gen_images.py
python3 scripts/compose_cards.py
python3 scripts/execute.py
```

产出后人工:检查 9 页卡 → 发布 → 把笔记链接回填 `note_url` → T+7 录入 feedback。

## 5. video 轨(后置,先跑通图文)

1. 复用 `assets/hero/{en}.png` 定妆照做图生视频(分镜 prompt 沿用 b.md:0-3s 挥手+字母 P 浮现 → 3-6s 字母扩展成单词 → 6-10s 拉全景转圈)
2. CosyVoice 女童声 TTS 4 句台词,句间 1-1.5s 静音留白(给孩子跟读反应时间),语速 0.9x
3. ffmpeg/剪映合成 9:16、15-20s,圆润卡通字幕,单独做带大字封面帧
4. 发布挂同款合集,正文导流到 wordcard 图文

## 6. resource 轨物料

- `products/virtual_pack/` 只放自制内容:按词族渲染的空白描红卡、单词闪卡(同一 Pillow 管线导出)、自制蜡笔动画、公版儿歌清单
- 笔记为"资源墙"九宫格 + 清单正文,钩子强调"原创/可打印/可投屏/持续更新",不承诺集数造假
- 9.9 成交路径:粉丝群群公告(前期)→ 小红书店铺(资质齐备后)

## 7. 成本与失败降级

- LLM:每词 2 次调用(台词 + 标题正文),Flash 模型单词成本可忽略
- 生图:每词 4 张,失败 2 次 → 该词 skipped,不影响其他词
- 字体缺失:脚本列出可用候选并退出,提示安装,不用默认位图字体硬出
- 全部词失败:produced.json 带 `error` 字段;已完成词不重复消耗生图额度

## 8. 发布安全清单(发布前逐项确认)

- [ ] 图上文字全部为后期叠加,无 AI 乱码字母
- [ ] 标题/正文/图片/评论无微信、二维码、外链、网盘字样
- [ ] 无知名 IP 形象与名称,资源均为自制/公版
- [ ] 标签含 3 个搜索大词;已归入对应词族合集
- [ ] 封面大字在缩略图尺寸(约 200px 宽)下仍清晰可读
